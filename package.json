{
  "name": "piccatch",
  "version": "4.0.0",
  "description": "PicCatch merchant tools for processing Excel catalogs, downloading images, and exporting ZIP archives.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "compression": "^1.7.5",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "playwright": "^1.55.0",
    "sharp": "^0.35.2"
  },
  "engines": {
    "node": ">=20"
  }
}
